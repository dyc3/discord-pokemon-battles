# Common Maintainence Tasks

## Updating the Battle API submodule

This command will update the Battle API git submodule to the latest commit in the upstream repo:

```bash
git submodule foreach git pull origin main
```
